name: Snyk test
on: push
jobs:
  security:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@master
    - uses: snyk/actions/setup@master
    #- uses: actions/setup-go@v1
     # with:
      #  go-version: "1.13"
          #- name: Snyk monitor
      #run: snyk monitor
    - name: Snyk test
      run: snyk test 
      env:
        SNYK_TOKEN: '6ca4ef19-e168-4dba-b0a5-6b06e297c7233'
      #--all-projects
      #sfdsf
        #SNYK_TOKEN: ${{ secrets.SNYK_TOKEN }}
    
      
